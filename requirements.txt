starlette>=0.40.0
uvicorn>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9
httpx>=0.27.0
# websockets is the upstream client our reverse proxy uses to connect to
# hermes's /api/pty, /api/ws, /api/events endpoints. httpx does not speak
# WebSockets, so we need a dedicated client library.
websockets>=12.0

# FastHTML for agent-built /projects/* mini-apps. Pure-Python frontend+backend
# (server-rendered HTML + HTMX), single-file per project. See server.py's
# project_dispatch handler.
python-fasthtml>=0.6.0
